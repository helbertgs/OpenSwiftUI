import Foundation

final class WindowServer {

    var isReady = false
    var displayProxy: DisplayProxy?

    init() {
    }

    func open(window: HostingWindow) throws {
    }

    func close(window: HostingWindow) {
    }

    func stop() {
    }

    func pollEvents() {
    }
}